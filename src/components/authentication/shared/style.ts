import { globalInnerSection } from '../../../data/global/variables';
import { StyleSheet } from 'aphrodite';

export const styles = StyleSheet.create({
    innerSection: globalInnerSection,
    blueGradient: {
        padding: '48px 41px 48px 36px',
        minHeight: 300,
    },
    iconPlaceholder: {
        margin: '0 auto 24px',
        width: '100%',
        borderRadius: 30
    },
    quotesBackground: {

    },
    quoteTitle: {
        color: '#fff',
        fontSize: 24,
        fontWeight: 300,
        marginTop: 24
    },
    quoteAuthor: {
        color: '#fff'
    }
});
